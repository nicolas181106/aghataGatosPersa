---
import Layout from "../layouts/layout.astro";
import oddie from "../assets/img/oddie.jpg";
import blube from "../assets/img/blube.jpg";
import ceres from "../assets/img/ceres.jpg";

import { gatosEuropeo } from "../scripts/gatos-europeo anterior.js";
import { gatosChinchilla } from "../scripts/gatos-chinchilla anterior.js";
import { gatosHimalayo } from "../scripts/gatos-himalayo.js";
---

<Layout
  title="Camadas anteriores | Ágatha Gatos Persas"
  description="Descubre las camadas anteriores de gatos persas europeos, chinchilla e himalayos de nuestro criadero en Sevilla, con fotos de padres y crías."
  keywords="camadas anteriores gato persa, camadas persa europeo, camadas persa chinchilla, camadas persa himalayo, criadero gatos persas Sevilla"
>
  <main class="min-h-screen bg-white/90 text-[#2E2E2E] pt-20 pb-20">
    <section class="w-full bg-gradient-to-r from-[#FF6B6B] via-[#ff9f9f] to-[#FFC3C3] text-white">
      <div class="max-w-6xl mx-auto px-4 py-10 sm:py-12 flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
        <div class="space-y-4">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-white/80">
            Camadas anteriores · Transparencia y cariño
          </p>
          <h1 class="text-3xl md:text-4xl font-extrabold tracking-tight">
            Camadas anteriores de Ágatha Gatos Persas
          </h1>
          <p class="max-w-xl text-sm md:text-base text-white/90">
            Aquí puedes ver algunas de nuestras <strong>camadas anteriores de gatos persas</strong>. Cada camada ha sido criada en un entorno
            familiar, con padres seleccionados por carácter y salud. Estas fotos son parte de nuestra historia como criadero.
          </p>
        </div>
        <div class="flex flex-col gap-3 bg-white/10 rounded-2xl p-5 border border-white/40 text-sm">
          <p class="font-semibold text-white text-base sm:text-lg">Navega por tipo de persa</p>
          <div class="flex flex-wrap gap-3">
            <a href="#europeo" class="inline-flex items-center justify-center rounded-full border border-white text-white text-xs sm:text-sm font-semibold px-4 py-2 shadow hover:bg-white/10 transition-colors">
              Camadas persa europeo
            </a>
            <a href="#chinchilla" class="inline-flex items-center justify-center rounded-full border border-white text-white text-xs sm:text-sm font-semibold px-4 py-2 shadow hover:bg-white/10 transition-colors">
              Camadas persa chinchilla
            </a>
            <a href="#himalayo" class="inline-flex items-center justify-center rounded-full border border-white text-white text-xs sm:text-sm font-semibold px-4 py-2 shadow hover:bg-white/10 transition-colors">
              Camadas persa himalayo
            </a>
          </div>
          <p class="text-xs text-white/80">
            Si quieres que te avisemos de <strong>nuevas camadas</strong>, puedes escribirnos desde la sección de contacto.
          </p>
        </div>
      </div>
    </section>

    <section id="europeo" class="max-w-6xl mx-auto px-4 pt-16">
      <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between mb-8">
        <div>
          <h2 class="text-2xl sm:text-3xl font-bold text-[#FF6B6B]">
            Camada persa europeo – 26 de mayo
          </h2>
          <p class="mt-2 text-sm sm:text-base text-gray-700 max-w-xl">
            Esta camada de <strong>persa europeo</strong> nació en un ambiente familiar, con padres de carácter tranquilo y muy cariñoso.
            Aquí puedes ver a sus padres y a los bebés que ya disfrutan de su nuevo hogar.
          </p>
        </div>
        <p class="text-xs uppercase tracking-[0.25em] text-[#FF6B6B] font-semibold">
          Línea europea
        </p>
      </div>

      <section class="max-w-[800px] mx-auto px-4 py-6 mt-2 flex flex-col sm:flex-row gap-8 justify-center items-center">
        <figure class="text-center hover:scale-105 transition-all duration-300 ease-in-out hover:cursor-pointer" id="oddieImg">
          <h3 class="text-xl sm:text-2xl font-bold mb-2">Padre · Oddie</h3>
          <img
            src={oddie.src}
            alt="Padre gato persa europeo"
            loading="lazy"
            class="p-2 h-75 w-75 object-cover rounded-2xl bg-[#FF6B6B] shadow-lg"
          />
        </figure>
        <figure class="text-center hover:scale-105 transition-all duration-300 ease-in-out hover:cursor-pointer" id="blubeImg">
          <h3 class="text-xl sm:text-2xl font-bold mb-2">Madre · Blue</h3>
          <img
            src={blube.src}
            alt="Madre gato persa europeo"
            loading="lazy"
            class="p-2 h-75 w-75 object-cover rounded-2xl bg-[#FF6B6B] shadow-lg"
          />
        </figure>
      </section>

      <div class="max-w-4xl mx-auto mt-6 text-center">
        <p class="text-base sm:text-lg text-gray-700">
          Esta camada nació el <strong>26 de mayo</strong>. Todos los gatitos están ya en sus nuevas familias.
        </p>
      </div>

      <section class="max-w-6xl mx-auto px-4 py-10 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          gatosEuropeo.map((europeo) => (
            <article class="bg-white/95 p-6 rounded-2xl shadow-2xl flex flex-col items-center hover:scale-105 transition-transform duration-300 border border-[#ffd3d3]">
              <img
                src={europeo.img}
                alt={europeo.alt}
                class="h-80 w-full object-cover rounded-2xl bg-[#FF6B6B]"
                loading="lazy"
              />
              <h3 class="mt-4 text-xl font-bold text-[#2E2E2E]">{europeo.name}</h3>
              <p class="text-gray-700 mt-1 text-sm">
                <strong>Género:</strong> {europeo.gender}
              </p>
              <p class="text-gray-700 text-sm">
                <strong>Fecha de nacimiento:</strong> {europeo.birth}
              </p>
            </article>
          ))
        }
      </section>
    </section>

    {/* CAMADAS PERSA CHINCHILLA */}
    <section id="chinchilla" class="max-w-6xl mx-auto px-4 pt-16">
      <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between mb-8">
        <div>
          <h2 class="text-2xl sm:text-3xl font-bold text-[#FF6B6B]">
            Camada persa chinchilla – 22 de junio
          </h2>
          <p class="mt-2 text-sm sm:text-base text-gray-700 max-w-xl">
            Los <strong>persas chinchilla</strong> destacan por su manto luminoso y su carácter calmado. Esta camada fue muy especial
            para nosotros, con gatitos muy dulces y sociables.
          </p>
        </div>
        <p class="text-xs uppercase tracking-[0.25em] text-[#FF6B6B] font-semibold">
          Línea chinchilla
        </p>
      </div>

      <section class="max-w-[800px] mx-auto px-4 py-6 mt-2 flex flex-col sm:flex-row gap-8 justify-center items-center">
        <figure class="text-center hover:scale-105 transition-all duration-300 ease-in-out hover:cursor-pointer">
          <h3 class="text-xl sm:text-2xl font-bold mb-2">Padre · Oddie</h3>
          <img
            src={oddie.src}
            alt="Padre gato persa chinchilla"
            loading="lazy"
            class="p-2 h-75 w-75 object-cover rounded-2xl bg-[#FF6B6B] shadow-lg"
          />
        </figure>
        <figure class="text-center hover:scale-105 transition-all duration-300 ease-in-out hover:cursor-pointer">
          <h3 class="text-xl sm:text-2xl font-bold mb-2">Madre · Ceres</h3>
          <img
            src={ceres.src}
            alt="Madre gato persa chinchilla"
            loading="lazy"
            class="p-2 h-75 w-75 object-cover rounded-2xl bg-[#FF6B6B] shadow-lg"
          />
        </figure>
      </section>

      <div class="max-w-4xl mx-auto mt-6 text-center">
        <p class="text-base sm:text-lg text-gray-700">
          Esta camada nació el <strong>22 de junio</strong>. Todos los chinchilla de esta camada están ya adoptados.
        </p>
      </div>

      <section class="max-w-6xl mx-auto px-4 py-10 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          gatosChinchilla.map((chinchilla) => (
            <article class="bg-white/95 p-6 rounded-2xl shadow-2xl flex flex-col items-center hover:scale-105 transition-transform duration-300 border border-[#ffd3d3]">
              <img
                src={chinchilla.img}
                alt={chinchilla.alt}
                class="h-80 w-full object-cover rounded-2xl bg-[#FF6B6B]"
                loading="lazy"
              />
              <h3 class="mt-4 text-xl font-bold text-[#2E2E2E]">{chinchilla.name}</h3>
              <p class="text-gray-700 mt-1 text-sm">
                <strong>Género:</strong> {chinchilla.gender}
              </p>
              <p class="text-gray-700 text-sm">
                <strong>Fecha de nacimiento:</strong> {chinchilla.birth}
              </p>
            </article>
          ))
        }
      </section>
    </section>

    {/* CAMADAS PERSA HIMALAYO */}
    <section id="himalayo" class="max-w-6xl mx-auto px-4 pt-16">
      <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between mb-6">
        <div>
          <h2 class="text-2xl sm:text-3xl font-bold text-[#FF6B6B]">
            Camadas persa himalayo
          </h2>
          <p class="mt-2 text-sm sm:text-base text-gray-700 max-w-xl">
            Estamos trabajando para incorporar <strong>camadas de persa himalayo</strong> a nuestro programa de cría. En cuanto tengamos
            noticias, actualizaremos esta sección con fotos y detalles.
          </p>
        </div>
        <p class="text-xs uppercase tracking-[0.25em] text-[#FF6B6B] font-semibold">
          Línea himalayo
        </p>
      </div>

      {gatosHimalayo.length === 0 && (
        <div class="max-w-4xl mx-auto bg-white/95 rounded-2xl shadow-2xl p-8 text-center">
          <p class="text-base sm:text-lg text-gray-700 mb-4">
            Por el momento, <strong>no se han registrado camadas</strong> de esta variedad en nuestro criadero.
          </p>
          <p class="text-sm text-gray-600 mb-6">
            Si te interesa especialmente el persa himalayo, puedes escribirnos para que te avisemos en cuanto tengamos novedades.
          </p>
          <a
            href="/contacto"
            class="inline-block bg-[#FF6B6B] hover:bg-[#e65c5c] text-white font-semibold py-3 px-6 rounded-full shadow transition duration-300 text-sm sm:text-base"
          >
            Avisadme de futuras camadas himalayo
          </a>
        </div>
      )}
    </section>
  </main>
</Layout>
